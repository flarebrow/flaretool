name: Auto Deploy

on:
#   push:
#     branches:
#       - main
    workflow_dispatch:
jobs:
    call-workflow:
        name: Update Docs
        runs-on: ubuntu-latest
        permissions:
          contents: write
          checks: write
          pull-requests: write
        step:
          - uses: actions/checkout@v3
          uses: .github/workflows/updatedoc.yml

